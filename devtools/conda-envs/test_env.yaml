name: test
channels:
  - conda-forge
dependencies:
    # Base depends
  - python
  - pip
  - sphinxcontrib-bibtex ==1.0

  - numpy
  - openmm >=7.5
  - pyyaml
  - scipy
  - pandas

    # Testing
  - isort
  - flake8
  - pytest
  - pytest-cov

  # Pip-only installs
  - pip:
    - codecov
