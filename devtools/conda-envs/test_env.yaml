name: test
channels:
  - conda-forge
dependencies:
    # Base depends
  - python
  - pip
  - sphinxcontrib-bibtex >=0.4

  - numpy
  - openmm >=7.5
  - pyyaml
  - scipy

    # Testing
  - isort
  - flake8
  - pytest
  - pytest-cov

  # Pip-only installs
  - pip:
    - codecov
