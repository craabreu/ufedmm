name: test
channels:
  - conda-forge
dependencies:
    # Base depends
  - pip
  - python
  - sphinxcontrib-bibtex ==1.0

  - numpy
  - openmm >=7.6
  - pandas
  - pyyaml
  - scipy
  - yaml

    # Testing
  - flake8
  - isort
  - pytest
  - pytest-cov

  # Pip-only installs
  - pip:
    - codecov
