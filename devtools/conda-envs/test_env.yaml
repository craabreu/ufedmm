name: test
channels:
  - conda-forge
dependencies:
    # Base depends
  - python
  - pip
  - numpy
  - openmm
  - pandas
  - pyyaml
  - scipy

    # Test depends
  - pytest
  - pytest-cov
  - codecov

    # Docs depends
  - sphinx
  - sphinxcontrib-bibtex
  - sphinx-copybutton
