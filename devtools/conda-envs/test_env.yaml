name: test
channels:
  - conda-forge
  - mdtools
dependencies:
    # Base depends
  - python
  - pip
  - numpy
  - openmm
  - pandas
  - pyyaml
  - scipy
  - cvpack

    # Test depends
  - pytest
  - pytest-cov
  - codecov

    # Docs depends
  - sphinx
  - sphinxcontrib-bibtex
  - sphinx-copybutton
