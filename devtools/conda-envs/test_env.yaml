name: test
channels:
  - conda-forge
  - omnia
dependencies:
    # Base depends
  - python
  - pip
  - sphinxcontrib-bibtex >=0.4

  - numpy
  - openmm >=7.3.1
  - pyyaml
  - scipy

    # Testing
  - isort
  - flake8
  - pytest
  - pytest-cov

  # Pip-only installs
  - pip:
    - codecov
